using CriandoEConsumindoArquivos.Models;
using CriandoEConsumindoArquivos.Services;

namespace CriandoEConsumindoArquivos;
public class Program
{
    static void Main(string[] args)
    {
        /*
        Console.Write("Entre com o caminho até o arquivo desejado: ");
        string caminho = Console.ReadLine()!;
        ManipuladorArquivoCSV veiculos = new ManipuladorArquivoCSV();
        List<Carro> listaVeiculo = veiculos.LerArquivo(caminho);

        foreach (var veiculo in listaVeiculo)
        {
            Console.WriteLine($"Marca: {veiculo.Marca} - Modelo: {veiculo.Modelo} - Cor: {veiculo.Cor} - Cavalos: {veiculo.Cavalos} - Valor: R${veiculo.Valor}");
        }
        
        ManipuladorArquivoCSV listaCriada = new ManipuladorArquivoCSV();
        listaCriada.EscreverArquivo();
        */
    }
}